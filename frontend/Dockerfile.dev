FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source (optional: bind mount will overwrite anyway)
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host"]
